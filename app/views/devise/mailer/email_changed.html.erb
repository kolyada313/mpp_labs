<p><%= t('messages.hello', email: @email) %></p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t('messages.email_change_pending', unconfirmed_email: @resource.unconfirmed_email) %></p>
<% else %>
  <p><%= t('messages.email_change_confirmed', email: @resource.email) %></p>
<% end %>
