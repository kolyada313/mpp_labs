<p><%= t('messages.hello', email: @resource.email) %></p>

<p><%= t('messages.account_locked') %></p>

<p><%= t('messages.unlock_account') %></p>

<p><%= link_to t('messages.unlock_my_account'), unlock_url(@resource, unlock_token: @token) %></p>
